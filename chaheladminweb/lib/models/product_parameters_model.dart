

class ProductParametersModel{
   String ? parametersID;
   double ? mrp;
   double ? rs;
   int ? stock;
   String ? uint;
   int? index; 
   ProductParametersModel({this.mrp,this.rs,this.stock,this.uint,this.parametersID,this.index});

  compareTo(int? index) {}
}